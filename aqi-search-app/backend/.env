# Copy to .env and fill values
PORT=5050
AQICN_TOKEN=Api_token

# Cache configuration
CACHE_TTL_SECONDS=300
CACHE_MAX_ENTRIES=500

# Optional: axios timeout in ms for provider calls
PROVIDER_TIMEOUT_MS=8000
